image: img:v10

variables:
    NAME: "abdessamad"
   

run:
  script:
    - echo "hello $NAME"
    - echo "this is SHA $CI_COMMIT_SHA"
    
  after_script:
    - ./status.sh $CI_COMMIT_SHA $token $CI_JOB_STATUS $CI_JOB_NAME $CI_JOB_URL
    
